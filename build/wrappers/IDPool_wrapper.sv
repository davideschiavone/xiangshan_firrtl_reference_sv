// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `IDPool` of class `IDPool`.
// Outer port list mirrors reference_sv/IDPool.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `IDPool` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import idpool_pkg::*;
module IDPool_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_free_valid,
  input  logic [2:0] io_free_bits,
  input  logic io_alloc_ready,
  output logic io_alloc_valid,
  output logic [2:0] io_alloc_bits
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  idpool_pkg::idpool_free_t free_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign free_i.valid = io_free_valid;
  assign free_i.bits = io_free_bits;

  // ---- Inner design (the LLM-authored readable) ----
  IDPool u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .alloc_ready_i(io_alloc_ready),
    .alloc_valid_o(io_alloc_valid),
    .alloc_bits_o(io_alloc_bits),
    .free_i(free_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
