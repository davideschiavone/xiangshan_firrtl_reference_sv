// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Iprio6Module_1` of class `Iprio6Module_1`.
// Outer port list mirrors reference_sv/Iprio6Module_1.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `Iprio6Module_1` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import iprio_pkg::*;
module Iprio6Module_1_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  input  logic sie_LC24IE,
  input  logic sie_LC25IE,
  input  logic sie_LC26IE,
  input  logic sie_LC27IE,
  input  logic sie_LC28IE,
  input  logic sie_LC29IE,
  input  logic sie_LC30IE,
  input  logic sie_LC31IE
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  iprio_pkg::sie_iprio6_subset_t sie_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign sie_i.LC24IE = sie_LC24IE;
  assign sie_i.LC25IE = sie_LC25IE;
  assign sie_i.LC26IE = sie_LC26IE;
  assign sie_i.LC27IE = sie_LC27IE;
  assign sie_i.LC28IE = sie_LC28IE;
  assign sie_i.LC29IE = sie_LC29IE;
  assign sie_i.LC30IE = sie_LC30IE;
  assign sie_i.LC31IE = sie_LC31IE;

  // ---- Inner design (the LLM-authored readable) ----
  Iprio6Module_1 u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .sie_i(sie_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
