// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `VtypeModule` of class `VtypeModule`.
// Outer port list mirrors reference_sv/VtypeModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `VtypeModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import rob_commit_csr_pkg::*;
module VtypeModule_wrapper (
  input  logic clock,
  input  logic reset,
  output logic [63:0] rdata,
  input  logic robCommit_vtype_valid,
  input  logic robCommit_vtype_bits_VILL,
  input  logic robCommit_vtype_bits_VMA,
  input  logic robCommit_vtype_bits_VTA,
  input  logic [2:0] robCommit_vtype_bits_VSEW,
  input  logic [2:0] robCommit_vtype_bits_VLMUL
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  rob_commit_csr_pkg::robCommit_vtype_subset_t robCommit_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign robCommit_i.vtype_valid = robCommit_vtype_valid;
  assign robCommit_i.vtype_bits_VILL = robCommit_vtype_bits_VILL;
  assign robCommit_i.vtype_bits_VMA = robCommit_vtype_bits_VMA;
  assign robCommit_i.vtype_bits_VTA = robCommit_vtype_bits_VTA;
  assign robCommit_i.vtype_bits_VSEW = robCommit_vtype_bits_VSEW;
  assign robCommit_i.vtype_bits_VLMUL = robCommit_vtype_bits_VLMUL;

  // ---- Inner design (the LLM-authored readable) ----
  VtypeModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .robCommit_i(robCommit_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
