// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Monitor` of class `Monitor`.
// Outer port list mirrors reference_sv/Monitor.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `Monitor` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import monitor_pkg::*;
module Monitor_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_fromMainPipe_task_s3_valid,
  input  logic [2:0] io_fromMainPipe_task_s3_bits_channel,
  input  logic io_fromMainPipe_task_s3_bits_mshrTask,
  input  logic io_fromMainPipe_dirResult_s3_hit,
  input  logic [1:0] io_fromMainPipe_dirResult_s3_meta_state,
  input  logic io_fromMainPipe_dirResult_s3_meta_clients
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  monitor_pkg::mainpipe_moni_subset_t fromMainPipe_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign fromMainPipe_i.task_s3_valid = io_fromMainPipe_task_s3_valid;
  assign fromMainPipe_i.task_s3_bits_channel = io_fromMainPipe_task_s3_bits_channel;
  assign fromMainPipe_i.task_s3_bits_mshrTask = io_fromMainPipe_task_s3_bits_mshrTask;
  assign fromMainPipe_i.dirResult_s3_hit = io_fromMainPipe_dirResult_s3_hit;
  assign fromMainPipe_i.dirResult_s3_meta_state = io_fromMainPipe_dirResult_s3_meta_state;
  assign fromMainPipe_i.dirResult_s3_meta_clients = io_fromMainPipe_dirResult_s3_meta_clients;

  // ---- Inner design (the LLM-authored readable) ----
  Monitor u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .fromMainPipe_i(fromMainPipe_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
