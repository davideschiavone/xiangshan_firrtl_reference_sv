// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `NewMgu` of class `NewMgu`.
// Outer port list mirrors reference_sv/NewMgu.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `NewMgu` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import newmgu_pkg::*;
module NewMgu_wrapper (
  input  logic [127:0] io_in_mask,
  input  logic io_in_info_ta,
  input  logic io_in_info_ma,
  input  logic [7:0] io_in_info_vl,
  input  logic [1:0] io_in_info_eew,
  input  logic [2:0] io_in_info_vdIdx,
  output logic [15:0] io_out_activeEn,
  output logic [15:0] io_out_agnosticEn
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  newmgu_pkg::newmgu_in_t in_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign in_i.mask = io_in_mask;
  assign in_i.info_ta = io_in_info_ta;
  assign in_i.info_ma = io_in_info_ma;
  assign in_i.info_vl = io_in_info_vl;
  assign in_i.info_eew = io_in_info_eew;
  assign in_i.info_vdIdx = io_in_info_vdIdx;

  // ---- Inner design (the LLM-authored readable) ----
  NewMgu u_inner (
    .activeEn_o(io_out_activeEn),
    .agnosticEn_o(io_out_agnosticEn),
    .in_i(in_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
