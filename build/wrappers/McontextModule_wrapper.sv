// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `McontextModule` of class `McontextModule`.
// Outer port list mirrors reference_sv/McontextModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `McontextModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
module McontextModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [13:0] rdata,
  output logic [13:0] regOut_HCONTEXT,
  input  logic fromHcontext_valid,
  input  logic [13:0] fromHcontext_bits_HCONTEXT,
  output logic [13:0] toHcontext_HCONTEXT
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::to_mcontext_t fromHcontext_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign fromHcontext_i.valid = fromHcontext_valid;
  assign fromHcontext_i.bits_HCONTEXT = fromHcontext_bits_HCONTEXT;

  // ---- Inner design (the LLM-authored readable) ----
  McontextModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .regOut_HCONTEXT_o(regOut_HCONTEXT),
    .toHcontext_HCONTEXT_o(toHcontext_HCONTEXT),
    .w_i(w_i),
    .fromHcontext_i(fromHcontext_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
