// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `LevelGateway` of class `LevelGateway`.
// Outer port list mirrors reference_sv/LevelGateway.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, it
// instantiates the parameterized readable `LevelGateway` with the
// pin values that distinguish this variant from its siblings.
// =====================================================================

module LevelGateway_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_interrupt,
  output logic io_plic_valid,
  input  logic io_plic_ready,
  input  logic io_plic_complete
);

  LevelGateway u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .interrupt_i(io_interrupt),
    .plic_valid_o(io_plic_valid),
    .plic_ready_i(io_plic_ready),
    .plic_complete_i(io_plic_complete)
  );

endmodule
