// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `HipModule` of class `HipModule`.
// Outer port list mirrors reference_sv/HipModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HipModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import h_interrupt_pkg::*;
import csr_common_pkg::*;
module HipModule_wrapper (
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_VSSIP,
  output logic regOut_VSTIP,
  output logic regOut_VSEIP,
  output logic regOut_SGEIP,
  input  logic mip_VSTIP,
  input  logic mip_VSEIP,
  input  logic mip_SGEIP,
  input  logic hvip_VSSIP,
  output logic toHvip_VSSIP_valid,
  output logic toHvip_VSSIP_bits
);

  // Output struct wire carrying the readable's CSR state.
  h_interrupt_pkg::hip_view_t hip_o;
  // Output struct wire carrying a write-forward bundle to a peer CSR.
  h_interrupt_pkg::hip_to_hvip_t toHvip_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  h_interrupt_pkg::mip_h_subset_t mip_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  h_interrupt_pkg::hvip_h_subset_t hvip_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign mip_i.VSTIP = mip_VSTIP;
  assign mip_i.VSEIP = mip_VSEIP;
  assign mip_i.SGEIP = mip_SGEIP;
  assign hvip_i.VSSIP = hvip_VSSIP;

  // ---- Inner design (the LLM-authored readable) ----
  HipModule u_inner (
    .rdata_o(rdata),
    .w_i(w_i),
    .mip_i(mip_i),
    .hvip_i(hvip_i),
    .hip_o(hip_o),
    .toHvip_o(toHvip_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_VSSIP = hip_o.VSSIP;
  assign regOut_VSTIP = hip_o.VSTIP;
  assign regOut_VSEIP = hip_o.VSEIP;
  assign regOut_SGEIP = hip_o.SGEIP;
  assign toHvip_VSSIP_valid = toHvip_o.VSSIP_valid;
  assign toHvip_VSSIP_bits = toHvip_o.VSSIP_bits;

endmodule
