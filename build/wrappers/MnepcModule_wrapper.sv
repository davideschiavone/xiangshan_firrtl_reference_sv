// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `MnepcModule` of class `SepcModule`.
// Outer port list mirrors reference_sv/MnepcModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `SepcModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import trap_event_pkg::*;
module MnepcModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic [62:0] regOut_epc,
  input  logic trapToMN_mnepc_valid,
  input  logic [62:0] trapToMN_mnepc_bits_epc
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  trap_event_pkg::trap_epc_event_t trap_epc_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign trap_epc_i.valid = trapToMN_mnepc_valid;
  assign trap_epc_i.bits_epc = trapToMN_mnepc_bits_epc;

  // ---- Inner design (the LLM-authored readable) ----
  SepcModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .regOut_epc_o(regOut_epc),
    .w_i(w_i),
    .trap_epc_i(trap_epc_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
