// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `ClockMux` of class `ClockMux`.
// Outer port list mirrors reference_sv/ClockMux.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, it
// instantiates the parameterized readable `ClockMux` with the
// pin values that distinguish this variant from its siblings.
// =====================================================================

module ClockMux_wrapper (
  input  logic clk0,
  input  logic clk1,
  input  logic sel,
  output logic clkout
);

  ClockMux u_inner (
    .clk0_i(clk0),
    .clk1_i(clk1),
    .sel_i(sel),
    .clkout_o(clkout)
  );

endmodule
