// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Iprio2Module_1` of class `Iprio2Module_1`.
// Outer port list mirrors reference_sv/Iprio2Module_1.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `Iprio2Module_1` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import iprio_pkg::*;
module Iprio2Module_1_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  input  logic sie_LCOFIE,
  input  logic sie_LC14IE,
  input  logic sie_LC15IE
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  iprio_pkg::sie_iprio2_subset_t sie_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign sie_i.LCOFIE = sie_LCOFIE;
  assign sie_i.LC14IE = sie_LC14IE;
  assign sie_i.LC15IE = sie_LC15IE;

  // ---- Inner design (the LLM-authored readable) ----
  Iprio2Module_1 u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .sie_i(sie_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
