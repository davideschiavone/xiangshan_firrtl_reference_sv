// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `BloomFilter` of class `BloomFilter`.
// Outer port list mirrors reference_sv/BloomFilter.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `BloomFilter` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import bloomfilter_pkg::*;
module BloomFilter_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_set_valid,
  input  logic [11:0] io_set_bits_addr,
  input  logic io_clr_valid,
  input  logic [11:0] io_clr_bits_addr
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  bloomfilter_pkg::bloom_query_t set_bits_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  bloomfilter_pkg::bloom_query_t clr_bits_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign set_bits_i.addr = io_set_bits_addr;
  assign clr_bits_i.addr = io_clr_bits_addr;

  // ---- Inner design (the LLM-authored readable) ----
  BloomFilter u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .set_valid_i(io_set_valid),
    .clr_valid_i(io_clr_valid),
    .set_bits_i(set_bits_i),
    .clr_bits_i(clr_bits_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
