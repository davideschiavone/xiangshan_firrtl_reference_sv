// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `ScounterenModule` of class `HcounterenModule`.
// Outer port list mirrors reference_sv/ScounterenModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HcounterenModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import hcounteren_pkg::*;
import csr_common_pkg::*;
module ScounterenModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_CY,
  output logic regOut_TM,
  output logic regOut_IR,
  output logic [28:0] regOut_HPM
);

  // Output struct wire carrying the readable's CSR state.
  hcounteren_pkg::hcounteren_t hcounteren_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  HcounterenModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .hcounteren_o(hcounteren_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_CY = hcounteren_o.CY;
  assign regOut_TM = hcounteren_o.TM;
  assign regOut_IR = hcounteren_o.IR;
  assign regOut_HPM = hcounteren_o.HPM;

endmodule
