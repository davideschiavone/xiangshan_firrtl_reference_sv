// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `SieModule` of class `SieModule`.
// Outer port list mirrors reference_sv/SieModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `SieModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import sie_pkg::*;
import h_interrupt_pkg::*;
import csr_common_pkg::*;
import mie_pkg::*;
import mvien_pkg::*;
import mideleg_pkg::*;
import interrupt_pkg::*;
module SieModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_SSIE,
  output logic regOut_STIE,
  output logic regOut_SEIE,
  output logic regOut_LCOFIE,
  output logic regOut_LC14IE,
  output logic regOut_LC15IE,
  output logic regOut_LC16IE,
  output logic regOut_LC17IE,
  output logic regOut_LC18IE,
  output logic regOut_LC19IE,
  output logic regOut_LC20IE,
  output logic regOut_LC21IE,
  output logic regOut_LC22IE,
  output logic regOut_LC23IE,
  output logic regOut_LC24IE,
  output logic regOut_LC25IE,
  output logic regOut_LC26IE,
  output logic regOut_LC27IE,
  output logic regOut_LC28IE,
  output logic regOut_LC29IE,
  output logic regOut_LC30IE,
  output logic regOut_LC31IE,
  output logic regOut_LC32IE,
  output logic regOut_LC33IE,
  output logic regOut_LC34IE,
  output logic regOut_LPRASEIE,
  output logic regOut_LC36IE,
  output logic regOut_LC37IE,
  output logic regOut_LC38IE,
  output logic regOut_LC39IE,
  output logic regOut_LC40IE,
  output logic regOut_LC41IE,
  output logic regOut_LC42IE,
  output logic regOut_HPRASEIE,
  output logic regOut_LC44IE,
  output logic regOut_LC45IE,
  output logic regOut_LC46IE,
  output logic regOut_LC47IE,
  output logic regOut_LC48IE,
  output logic regOut_LC49IE,
  output logic regOut_LC50IE,
  output logic regOut_LC51IE,
  output logic regOut_LC52IE,
  output logic regOut_LC53IE,
  output logic regOut_LC54IE,
  output logic regOut_LC55IE,
  output logic regOut_LC56IE,
  output logic regOut_LC57IE,
  output logic regOut_LC58IE,
  output logic regOut_LC59IE,
  output logic regOut_LC60IE,
  output logic regOut_LC61IE,
  output logic regOut_LC62IE,
  output logic regOut_LC63IE,
  input  logic mideleg_SSI,
  input  logic mideleg_STI,
  input  logic mideleg_SEI,
  input  logic mideleg_LCOFI,
  input  logic mie_SSIE,
  input  logic mie_VSSIE,
  input  logic mie_MSIE,
  input  logic mie_STIE,
  input  logic mie_VSTIE,
  input  logic mie_MTIE,
  input  logic mie_SEIE,
  input  logic mie_VSEIE,
  input  logic mie_MEIE,
  input  logic mie_SGEIE,
  input  logic mie_LCOFIE,
  input  logic mvien_SSIE,
  input  logic mvien_SEIE,
  input  logic mvien_LCOFIE,
  input  logic mvien_LC14IE,
  input  logic mvien_LC15IE,
  input  logic mvien_LC16IE,
  input  logic mvien_LC17IE,
  input  logic mvien_LC18IE,
  input  logic mvien_LC19IE,
  input  logic mvien_LC20IE,
  input  logic mvien_LC21IE,
  input  logic mvien_LC22IE,
  input  logic mvien_LC23IE,
  input  logic mvien_LC24IE,
  input  logic mvien_LC25IE,
  input  logic mvien_LC26IE,
  input  logic mvien_LC27IE,
  input  logic mvien_LC28IE,
  input  logic mvien_LC29IE,
  input  logic mvien_LC30IE,
  input  logic mvien_LC31IE,
  input  logic mvien_LC32IE,
  input  logic mvien_LC33IE,
  input  logic mvien_LC34IE,
  input  logic mvien_LPRASEIE,
  input  logic mvien_LC36IE,
  input  logic mvien_LC37IE,
  input  logic mvien_LC38IE,
  input  logic mvien_LC39IE,
  input  logic mvien_LC40IE,
  input  logic mvien_LC41IE,
  input  logic mvien_LC42IE,
  input  logic mvien_HPRASEIE,
  input  logic mvien_LC44IE,
  input  logic mvien_LC45IE,
  input  logic mvien_LC46IE,
  input  logic mvien_LC47IE,
  input  logic mvien_LC48IE,
  input  logic mvien_LC49IE,
  input  logic mvien_LC50IE,
  input  logic mvien_LC51IE,
  input  logic mvien_LC52IE,
  input  logic mvien_LC53IE,
  input  logic mvien_LC54IE,
  input  logic mvien_LC55IE,
  input  logic mvien_LC56IE,
  input  logic mvien_LC57IE,
  input  logic mvien_LC58IE,
  input  logic mvien_LC59IE,
  input  logic mvien_LC60IE,
  input  logic mvien_LC61IE,
  input  logic mvien_LC62IE,
  input  logic mvien_LC63IE,
  output logic toMie_SSIE_valid,
  output logic toMie_SSIE_bits,
  output logic toMie_STIE_valid,
  output logic toMie_STIE_bits,
  output logic toMie_SEIE_valid,
  output logic toMie_SEIE_bits,
  output logic toMie_LCOFIE_valid,
  output logic toMie_LCOFIE_bits,
  input  logic fromVSie_LCOFIE_valid,
  input  logic fromVSie_LCOFIE_bits
);

  // Output struct wire carrying the readable's CSR state.
  sie_pkg::sie_t sie_o;
  // Output struct wire carrying a write-forward bundle to a peer CSR.
  h_interrupt_pkg::sie_to_mie_t toMie_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  mie_pkg::mie_t mie_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  mvien_pkg::mvien_t mvien_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  mideleg_pkg::mideleg_t mideleg_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  interrupt_pkg::vsie_to_sie_t fromVSie_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign mideleg_i.SSI = mideleg_SSI;
  assign mideleg_i.STI = mideleg_STI;
  assign mideleg_i.SEI = mideleg_SEI;
  assign mideleg_i.LCOFI = mideleg_LCOFI;
  assign mie_i.SSIE = mie_SSIE;
  assign mie_i.VSSIE = mie_VSSIE;
  assign mie_i.MSIE = mie_MSIE;
  assign mie_i.STIE = mie_STIE;
  assign mie_i.VSTIE = mie_VSTIE;
  assign mie_i.MTIE = mie_MTIE;
  assign mie_i.SEIE = mie_SEIE;
  assign mie_i.VSEIE = mie_VSEIE;
  assign mie_i.MEIE = mie_MEIE;
  assign mie_i.SGEIE = mie_SGEIE;
  assign mie_i.LCOFIE = mie_LCOFIE;
  assign mvien_i.SSIE = mvien_SSIE;
  assign mvien_i.SEIE = mvien_SEIE;
  assign mvien_i.LCOFIE = mvien_LCOFIE;
  assign mvien_i.LC14IE = mvien_LC14IE;
  assign mvien_i.LC15IE = mvien_LC15IE;
  assign mvien_i.LC16IE = mvien_LC16IE;
  assign mvien_i.LC17IE = mvien_LC17IE;
  assign mvien_i.LC18IE = mvien_LC18IE;
  assign mvien_i.LC19IE = mvien_LC19IE;
  assign mvien_i.LC20IE = mvien_LC20IE;
  assign mvien_i.LC21IE = mvien_LC21IE;
  assign mvien_i.LC22IE = mvien_LC22IE;
  assign mvien_i.LC23IE = mvien_LC23IE;
  assign mvien_i.LC24IE = mvien_LC24IE;
  assign mvien_i.LC25IE = mvien_LC25IE;
  assign mvien_i.LC26IE = mvien_LC26IE;
  assign mvien_i.LC27IE = mvien_LC27IE;
  assign mvien_i.LC28IE = mvien_LC28IE;
  assign mvien_i.LC29IE = mvien_LC29IE;
  assign mvien_i.LC30IE = mvien_LC30IE;
  assign mvien_i.LC31IE = mvien_LC31IE;
  assign mvien_i.LC32IE = mvien_LC32IE;
  assign mvien_i.LC33IE = mvien_LC33IE;
  assign mvien_i.LC34IE = mvien_LC34IE;
  assign mvien_i.LPRASEIE = mvien_LPRASEIE;
  assign mvien_i.LC36IE = mvien_LC36IE;
  assign mvien_i.LC37IE = mvien_LC37IE;
  assign mvien_i.LC38IE = mvien_LC38IE;
  assign mvien_i.LC39IE = mvien_LC39IE;
  assign mvien_i.LC40IE = mvien_LC40IE;
  assign mvien_i.LC41IE = mvien_LC41IE;
  assign mvien_i.LC42IE = mvien_LC42IE;
  assign mvien_i.HPRASEIE = mvien_HPRASEIE;
  assign mvien_i.LC44IE = mvien_LC44IE;
  assign mvien_i.LC45IE = mvien_LC45IE;
  assign mvien_i.LC46IE = mvien_LC46IE;
  assign mvien_i.LC47IE = mvien_LC47IE;
  assign mvien_i.LC48IE = mvien_LC48IE;
  assign mvien_i.LC49IE = mvien_LC49IE;
  assign mvien_i.LC50IE = mvien_LC50IE;
  assign mvien_i.LC51IE = mvien_LC51IE;
  assign mvien_i.LC52IE = mvien_LC52IE;
  assign mvien_i.LC53IE = mvien_LC53IE;
  assign mvien_i.LC54IE = mvien_LC54IE;
  assign mvien_i.LC55IE = mvien_LC55IE;
  assign mvien_i.LC56IE = mvien_LC56IE;
  assign mvien_i.LC57IE = mvien_LC57IE;
  assign mvien_i.LC58IE = mvien_LC58IE;
  assign mvien_i.LC59IE = mvien_LC59IE;
  assign mvien_i.LC60IE = mvien_LC60IE;
  assign mvien_i.LC61IE = mvien_LC61IE;
  assign mvien_i.LC62IE = mvien_LC62IE;
  assign mvien_i.LC63IE = mvien_LC63IE;
  assign fromVSie_i.LCOFIE_valid = fromVSie_LCOFIE_valid;
  assign fromVSie_i.LCOFIE_bits = fromVSie_LCOFIE_bits;
  // (FIRRTL DCE'd `fromVSie_HPRASEIE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.HPRASEIE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_HPRASEIE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.HPRASEIE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC14IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC14IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC14IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC14IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC15IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC15IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC15IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC15IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC16IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC16IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC16IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC16IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC17IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC17IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC17IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC17IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC18IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC18IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC18IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC18IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC19IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC19IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC19IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC19IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC20IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC20IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC20IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC20IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC21IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC21IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC21IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC21IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC22IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC22IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC22IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC22IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC23IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC23IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC23IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC23IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC24IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC24IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC24IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC24IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC25IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC25IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC25IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC25IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC26IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC26IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC26IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC26IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC27IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC27IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC27IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC27IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC28IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC28IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC28IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC28IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC29IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC29IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC29IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC29IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC30IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC30IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC30IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC30IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC31IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC31IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC31IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC31IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC32IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC32IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC32IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC32IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC33IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC33IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC33IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC33IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC34IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC34IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC34IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC34IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC36IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC36IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC36IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC36IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC37IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC37IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC37IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC37IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC38IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC38IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC38IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC38IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC39IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC39IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC39IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC39IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC40IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC40IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC40IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC40IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC41IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC41IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC41IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC41IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC42IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC42IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC42IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC42IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC44IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC44IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC44IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC44IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC45IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC45IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC45IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC45IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC46IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC46IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC46IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC46IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC47IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC47IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC47IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC47IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC48IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC48IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC48IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC48IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC49IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC49IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC49IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC49IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC50IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC50IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC50IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC50IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC51IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC51IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC51IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC51IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC52IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC52IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC52IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC52IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC53IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC53IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC53IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC53IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC54IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC54IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC54IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC54IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC55IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC55IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC55IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC55IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC56IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC56IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC56IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC56IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC57IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC57IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC57IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC57IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC58IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC58IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC58IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC58IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC59IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC59IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC59IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC59IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC60IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC60IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC60IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC60IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC61IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC61IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC61IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC61IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC62IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC62IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC62IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC62IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LC63IE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC63IE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LC63IE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LC63IE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_LPRASEIE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LPRASEIE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_LPRASEIE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.LPRASEIE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_VSEIE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSEIE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_VSEIE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSEIE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_VSSIE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSSIE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_VSSIE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSSIE_valid = '0;
  // (FIRRTL DCE'd `fromVSie_VSTIE_bits` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSTIE_bits = '0;
  // (FIRRTL DCE'd `fromVSie_VSTIE_valid` — readable doesn't consume it; tie to 0)
  assign fromVSie_i.VSTIE_valid = '0;

  // ---- Inner design (the LLM-authored readable) ----
  SieModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .mie_i(mie_i),
    .mvien_i(mvien_i),
    .mideleg_i(mideleg_i),
    .fromVSie_i(fromVSie_i),
    .sie_o(sie_o),
    .toMie_o(toMie_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_SSIE = sie_o.SSIE;
  assign regOut_STIE = sie_o.STIE;
  assign regOut_SEIE = sie_o.SEIE;
  assign regOut_LCOFIE = sie_o.LCOFIE;
  assign regOut_LC14IE = sie_o.LC14IE;
  assign regOut_LC15IE = sie_o.LC15IE;
  assign regOut_LC16IE = sie_o.LC16IE;
  assign regOut_LC17IE = sie_o.LC17IE;
  assign regOut_LC18IE = sie_o.LC18IE;
  assign regOut_LC19IE = sie_o.LC19IE;
  assign regOut_LC20IE = sie_o.LC20IE;
  assign regOut_LC21IE = sie_o.LC21IE;
  assign regOut_LC22IE = sie_o.LC22IE;
  assign regOut_LC23IE = sie_o.LC23IE;
  assign regOut_LC24IE = sie_o.LC24IE;
  assign regOut_LC25IE = sie_o.LC25IE;
  assign regOut_LC26IE = sie_o.LC26IE;
  assign regOut_LC27IE = sie_o.LC27IE;
  assign regOut_LC28IE = sie_o.LC28IE;
  assign regOut_LC29IE = sie_o.LC29IE;
  assign regOut_LC30IE = sie_o.LC30IE;
  assign regOut_LC31IE = sie_o.LC31IE;
  assign regOut_LC32IE = sie_o.LC32IE;
  assign regOut_LC33IE = sie_o.LC33IE;
  assign regOut_LC34IE = sie_o.LC34IE;
  assign regOut_LPRASEIE = sie_o.LPRASEIE;
  assign regOut_LC36IE = sie_o.LC36IE;
  assign regOut_LC37IE = sie_o.LC37IE;
  assign regOut_LC38IE = sie_o.LC38IE;
  assign regOut_LC39IE = sie_o.LC39IE;
  assign regOut_LC40IE = sie_o.LC40IE;
  assign regOut_LC41IE = sie_o.LC41IE;
  assign regOut_LC42IE = sie_o.LC42IE;
  assign regOut_HPRASEIE = sie_o.HPRASEIE;
  assign regOut_LC44IE = sie_o.LC44IE;
  assign regOut_LC45IE = sie_o.LC45IE;
  assign regOut_LC46IE = sie_o.LC46IE;
  assign regOut_LC47IE = sie_o.LC47IE;
  assign regOut_LC48IE = sie_o.LC48IE;
  assign regOut_LC49IE = sie_o.LC49IE;
  assign regOut_LC50IE = sie_o.LC50IE;
  assign regOut_LC51IE = sie_o.LC51IE;
  assign regOut_LC52IE = sie_o.LC52IE;
  assign regOut_LC53IE = sie_o.LC53IE;
  assign regOut_LC54IE = sie_o.LC54IE;
  assign regOut_LC55IE = sie_o.LC55IE;
  assign regOut_LC56IE = sie_o.LC56IE;
  assign regOut_LC57IE = sie_o.LC57IE;
  assign regOut_LC58IE = sie_o.LC58IE;
  assign regOut_LC59IE = sie_o.LC59IE;
  assign regOut_LC60IE = sie_o.LC60IE;
  assign regOut_LC61IE = sie_o.LC61IE;
  assign regOut_LC62IE = sie_o.LC62IE;
  assign regOut_LC63IE = sie_o.LC63IE;
  assign toMie_SSIE_valid = toMie_o.SSIE_valid;
  assign toMie_SSIE_bits = toMie_o.SSIE_bits;
  assign toMie_STIE_valid = toMie_o.STIE_valid;
  assign toMie_STIE_bits = toMie_o.STIE_bits;
  assign toMie_SEIE_valid = toMie_o.SEIE_valid;
  assign toMie_SEIE_bits = toMie_o.SEIE_bits;
  assign toMie_LCOFIE_valid = toMie_o.LCOFIE_valid;
  assign toMie_LCOFIE_bits = toMie_o.LCOFIE_bits;

endmodule
