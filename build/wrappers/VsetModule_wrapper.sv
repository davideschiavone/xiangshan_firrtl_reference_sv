// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `VsetModule` of class `VsetModule`.
// Outer port list mirrors reference_sv/VsetModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `VsetModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
module VsetModule_wrapper (
  input  logic [63:0] io_in_avl,
  input  logic io_in_vtype_illegal,
  input  logic [54:0] io_in_vtype_reserved,
  input  logic io_in_vtype_vma,
  input  logic io_in_vtype_vta,
  input  logic [2:0] io_in_vtype_vsew,
  input  logic [2:0] io_in_vtype_vlmul,
  input  logic [8:0] io_in_func,
  output logic io_out_vconfig_vtype_illegal,
  output logic io_out_vconfig_vtype_vma,
  output logic io_out_vconfig_vtype_vta,
  output logic [1:0] io_out_vconfig_vtype_vsew,
  output logic [2:0] io_out_vconfig_vtype_vlmul,
  output logic [7:0] io_out_vconfig_vl,
  output logic [7:0] io_out_vlmax
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  csr_common_pkg::vconfig_t vconfig_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::vtype_t vtype_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign vtype_i.illegal = io_in_vtype_illegal;
  assign vtype_i.reserved = io_in_vtype_reserved;
  assign vtype_i.vma = io_in_vtype_vma;
  assign vtype_i.vta = io_in_vtype_vta;
  assign vtype_i.vsew = io_in_vtype_vsew;
  assign vtype_i.vlmul = io_in_vtype_vlmul;

  // ---- Inner design (the LLM-authored readable) ----
  VsetModule u_inner (
    .avl_i(io_in_avl),
    .func_i(io_in_func),
    .vlmax_o(io_out_vlmax),
    .vtype_i(vtype_i),
    .vconfig_o(vconfig_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_out_vconfig_vtype_illegal = vconfig_o.vtype_illegal;
  assign io_out_vconfig_vtype_vma = vconfig_o.vtype_vma;
  assign io_out_vconfig_vtype_vta = vconfig_o.vtype_vta;
  assign io_out_vconfig_vtype_vsew = vconfig_o.vtype_vsew;
  assign io_out_vconfig_vtype_vlmul = vconfig_o.vtype_vlmul;
  assign io_out_vconfig_vl = vconfig_o.vl;

endmodule
