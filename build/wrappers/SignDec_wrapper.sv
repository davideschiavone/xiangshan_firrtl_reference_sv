// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `SignDec` of class `SignDec`.
// Outer port list mirrors reference_sv/SignDec.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `SignDec` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================

module SignDec_wrapper (
  input  logic io_sign_0,
  input  logic io_sign_1,
  input  logic io_sign_2,
  input  logic io_sign_3,
  output logic [4:0] io_q
);

  // Unpacked-array wire — Chisel Vec(4) input port.
  logic sign_i [4];

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign sign_i[0] = io_sign_0;
  assign sign_i[1] = io_sign_1;
  assign sign_i[2] = io_sign_2;
  assign sign_i[3] = io_sign_3;

  // ---- Inner design (the LLM-authored readable) ----
  SignDec u_inner (
    .q_o(io_q),
    .sign_i(sign_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
