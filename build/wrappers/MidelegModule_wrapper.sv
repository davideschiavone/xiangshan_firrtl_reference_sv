// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `MidelegModule` of class `MidelegModule`.
// Outer port list mirrors reference_sv/MidelegModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `MidelegModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import mideleg_pkg::*;
import csr_common_pkg::*;
module MidelegModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_SSI,
  output logic regOut_STI,
  output logic regOut_SEI,
  output logic regOut_LCOFI
);

  // Output struct wire carrying the readable's CSR state.
  mideleg_pkg::mideleg_t mideleg_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  MidelegModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .mideleg_o(mideleg_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_SSI = mideleg_o.SSI;
  assign regOut_STI = mideleg_o.STI;
  assign regOut_SEI = mideleg_o.SEI;
  assign regOut_LCOFI = mideleg_o.LCOFI;

endmodule
