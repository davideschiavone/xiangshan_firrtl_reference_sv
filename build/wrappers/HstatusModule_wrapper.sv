// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `HstatusModule` of class `HstatusModule`.
// Outer port list mirrors reference_sv/HstatusModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HstatusModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import hstatus_pkg::*;
import csr_common_pkg::*;
import trap_event_pkg::*;
module HstatusModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_GVA,
  output logic regOut_SPV,
  output logic regOut_SPVP,
  output logic regOut_HU,
  output logic [5:0] regOut_VGEIN,
  output logic regOut_VTVM,
  output logic regOut_VTW,
  output logic regOut_VTSR,
  output logic [1:0] regOut_HUPMM,
  input  logic retFromS_hstatus_valid,
  input  logic retFromS_hstatus_bits_SPV,
  input  logic trapToHS_hstatus_valid,
  input  logic trapToHS_hstatus_bits_GVA,
  input  logic trapToHS_hstatus_bits_SPV,
  input  logic trapToHS_hstatus_bits_SPVP
);

  // Output struct wire carrying the readable's CSR state.
  hstatus_pkg::hstatus_t hstatus_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  trap_event_pkg::trap_hstatus_event_t ret_hstatus_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  trap_event_pkg::trap_hstatus_event_t trap_hstatus_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign ret_hstatus_i.valid = retFromS_hstatus_valid;
  assign ret_hstatus_i.bits_SPV = retFromS_hstatus_bits_SPV;
  assign trap_hstatus_i.valid = trapToHS_hstatus_valid;
  assign trap_hstatus_i.bits_GVA = trapToHS_hstatus_bits_GVA;
  assign trap_hstatus_i.bits_SPV = trapToHS_hstatus_bits_SPV;
  assign trap_hstatus_i.bits_SPVP = trapToHS_hstatus_bits_SPVP;
  // (FIRRTL DCE'd `ret_hstatus_bits_GVA` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_GVA = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_HU` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_HU = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_HUPMM` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_HUPMM = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_SPVP` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_SPVP = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VGEIN` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VGEIN = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VSBE` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VSBE = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VSXL` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VSXL = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VTSR` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VTSR = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VTVM` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VTVM = '0;
  // (FIRRTL DCE'd `ret_hstatus_bits_VTW` — readable doesn't consume it; tie to 0)
  assign ret_hstatus_i.bits_VTW = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_HU` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_HU = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_HUPMM` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_HUPMM = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VGEIN` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VGEIN = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VSBE` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VSBE = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VSXL` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VSXL = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VTSR` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VTSR = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VTVM` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VTVM = '0;
  // (FIRRTL DCE'd `trap_hstatus_bits_VTW` — readable doesn't consume it; tie to 0)
  assign trap_hstatus_i.bits_VTW = '0;

  // ---- Inner design (the LLM-authored readable) ----
  HstatusModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .ret_hstatus_i(ret_hstatus_i),
    .trap_hstatus_i(trap_hstatus_i),
    .hstatus_o(hstatus_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_GVA = hstatus_o.GVA;
  assign regOut_SPV = hstatus_o.SPV;
  assign regOut_SPVP = hstatus_o.SPVP;
  assign regOut_HU = hstatus_o.HU;
  assign regOut_VGEIN = hstatus_o.VGEIN;
  assign regOut_VTVM = hstatus_o.VTVM;
  assign regOut_VTW = hstatus_o.VTW;
  assign regOut_VTSR = hstatus_o.VTSR;
  assign regOut_HUPMM = hstatus_o.HUPMM;

endmodule
