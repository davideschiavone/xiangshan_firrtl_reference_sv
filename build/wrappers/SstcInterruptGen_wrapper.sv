// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `SstcInterruptGen` of class `SstcInterruptGen`.
// Outer port list mirrors reference_sv/SstcInterruptGen.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `SstcInterruptGen` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import sstc_pkg::*;
module SstcInterruptGen_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic i_stime_valid,
  input  logic [63:0] i_stime_bits,
  input  logic i_vstime_valid,
  input  logic [63:0] i_vstime_bits,
  input  logic i_stimecmp_wen,
  input  logic [63:0] i_stimecmp_rdata,
  input  logic i_vstimecmp_wen,
  input  logic [63:0] i_vstimecmp_rdata,
  input  logic i_htimedeltaWen,
  input  logic i_menvcfg_wen,
  input  logic i_menvcfg_STCE,
  input  logic i_henvcfg_wen,
  input  logic i_henvcfg_STCE,
  output logic o_STIP,
  output logic o_VSTIP
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  sstc_pkg::sstc_out_t o_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  sstc_pkg::sstc_in_t i_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign i_i.stime_valid = i_stime_valid;
  assign i_i.stime_bits = i_stime_bits;
  assign i_i.vstime_valid = i_vstime_valid;
  assign i_i.vstime_bits = i_vstime_bits;
  assign i_i.stimecmp_wen = i_stimecmp_wen;
  assign i_i.stimecmp_rdata = i_stimecmp_rdata;
  assign i_i.vstimecmp_wen = i_vstimecmp_wen;
  assign i_i.vstimecmp_rdata = i_vstimecmp_rdata;
  assign i_i.htimedeltaWen = i_htimedeltaWen;
  assign i_i.menvcfg_wen = i_menvcfg_wen;
  assign i_i.menvcfg_STCE = i_menvcfg_STCE;
  assign i_i.henvcfg_wen = i_henvcfg_wen;
  assign i_i.henvcfg_STCE = i_henvcfg_STCE;

  // ---- Inner design (the LLM-authored readable) ----
  SstcInterruptGen u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .i_i(i_i),
    .o_o(o_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign o_STIP = o_o.STIP;
  assign o_VSTIP = o_o.VSTIP;

endmodule
