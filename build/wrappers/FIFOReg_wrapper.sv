// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `FIFOReg` of class `FIFOReg`.
// Outer port list mirrors reference_sv/FIFOReg.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, it
// instantiates the parameterized readable `FIFOReg` with the
// pin values that distinguish this variant from its siblings.
// =====================================================================

module FIFOReg_wrapper (
  input  logic clock,
  input  logic reset,
  output logic io_enq_ready,
  input  logic io_enq_valid,
  input  logic [3:0] io_enq_bits,
  input  logic io_deq_ready,
  output logic io_deq_valid,
  output logic [3:0] io_deq_bits,
  input  logic io_flush
);

  FIFOReg u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .enq_ready_o(io_enq_ready),
    .enq_valid_i(io_enq_valid),
    .enq_bits_i(io_enq_bits),
    .deq_ready_i(io_deq_ready),
    .deq_valid_o(io_deq_valid),
    .deq_bits_o(io_deq_bits),
    .flush_i(io_flush)
  );

endmodule
