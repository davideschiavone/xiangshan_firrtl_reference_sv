// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `BranchModule` of class `BranchModule`.
// Outer port list mirrors reference_sv/BranchModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `BranchModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================

module BranchModule_wrapper (
  input  logic [63:0] io_src_0,
  input  logic [63:0] io_src_1,
  input  logic [8:0] io_func,
  input  logic io_fixedTaken,
  output logic io_taken,
  output logic io_mispredict
);

  // Unpacked-array wire — Chisel Vec(2) input port.
  logic [64-1:0] src_i [2];

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign src_i[0] = io_src_0;
  assign src_i[1] = io_src_1;

  // ---- Inner design (the LLM-authored readable) ----
  BranchModule u_inner (
    .func_i(io_func),
    .fixedTaken_i(io_fixedTaken),
    .taken_o(io_taken),
    .mispredict_o(io_mispredict),
    .src_i(src_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
