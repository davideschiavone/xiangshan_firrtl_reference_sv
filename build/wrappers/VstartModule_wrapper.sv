// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `VstartModule` of class `VstartModule`.
// Outer port list mirrors reference_sv/VstartModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `VstartModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import rob_commit_csr_pkg::*;
module VstartModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic [6:0] regOut_vstart,
  input  logic robCommit_vsDirty,
  input  logic robCommit_vstart_valid,
  input  logic [6:0] robCommit_vstart_bits
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  rob_commit_csr_pkg::robCommit_vstart_subset_t robCommit_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign robCommit_i.vsDirty = robCommit_vsDirty;
  assign robCommit_i.vstart_valid = robCommit_vstart_valid;
  assign robCommit_i.vstart_bits = robCommit_vstart_bits;

  // ---- Inner design (the LLM-authored readable) ----
  VstartModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .regOut_vstart_o(regOut_vstart),
    .w_i(w_i),
    .robCommit_i(robCommit_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
