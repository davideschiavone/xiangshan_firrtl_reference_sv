// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Pma23cfgModule` of class `Pma0cfgModule`.
// Outer port list mirrors reference_sv/Pma23cfgModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `Pma0cfgModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import pma0cfg_pkg::*;
import csr_common_pkg::*;
module Pma23cfgModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [7:0] rdata,
  output logic regOut_R,
  output logic regOut_W,
  output logic regOut_X,
  output logic [1:0] regOut_A,
  output logic regOut_ATOMIC,
  output logic regOut_C,
  output logic regOut_L
);

  // Output struct wire carrying the readable's CSR state.
  pma0cfg_pkg::pma0cfg_t pma0cfg_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  Pma0cfgModule #( .INIT_VALUE(8'h0b) ) u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .pma0cfg_o(pma0cfg_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_R = pma0cfg_o.R;
  assign regOut_W = pma0cfg_o.W;
  assign regOut_X = pma0cfg_o.X;
  assign regOut_A = pma0cfg_o.A;
  assign regOut_ATOMIC = pma0cfg_o.ATOMIC;
  assign regOut_C = pma0cfg_o.C;
  assign regOut_L = pma0cfg_o.L;

endmodule
