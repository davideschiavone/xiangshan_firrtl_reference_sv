// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `ResetGen` of class `ResetGen`.
// Outer port list mirrors reference_sv/ResetGen.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, it
// instantiates the parameterized readable `ResetGen` with the
// pin values that distinguish this variant from its siblings.
// =====================================================================

module ResetGen_wrapper (
  input  logic clock,
  input  logic reset,
  output logic o_reset
);

  ResetGen u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .o_reset_o(o_reset)
  );

endmodule
