// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `CVT32ModuleS2` of class `CVT32ModuleS2`.
// Outer port list mirrors reference_sv/CVT32ModuleS2.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `CVT32ModuleS2` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import cvt32_pkg::*;
module CVT32ModuleS2_wrapper (
  input  logic [31:0] io_s2In_result,
  input  logic [4:0] io_s2In_fflags,
  output logic [31:0] io_s2Out_result,
  output logic [4:0] io_s2Out_fflags
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  cvt32_pkg::cvt32_stage_t s2Out_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  cvt32_pkg::cvt32_stage_t s2In_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign s2In_i.result = io_s2In_result;
  assign s2In_i.fflags = io_s2In_fflags;

  // ---- Inner design (the LLM-authored readable) ----
  CVT32ModuleS2 u_inner (
    .s2In_i(s2In_i),
    .s2Out_o(s2Out_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_s2Out_result = s2Out_o.result;
  assign io_s2Out_fflags = s2Out_o.fflags;

endmodule
