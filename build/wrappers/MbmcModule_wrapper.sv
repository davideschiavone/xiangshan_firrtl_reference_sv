// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `MbmcModule` of class `MbmcModule`.
// Outer port list mirrors reference_sv/MbmcModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `MbmcModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import mbmc_pkg::*;
import csr_common_pkg::*;
module MbmcModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic [57:0] regOut_BMA,
  output logic regOut_KEYIDEN,
  output logic regOut_BME,
  output logic regOut_BCLEAR,
  output logic regOut_CMODE
);

  // Output struct wire carrying the readable's CSR state.
  mbmc_pkg::mbmc_t mbmc_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  MbmcModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .mbmc_o(mbmc_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_BMA = mbmc_o.BMA;
  assign regOut_KEYIDEN = mbmc_o.KEYIDEN;
  assign regOut_BME = mbmc_o.BME;
  assign regOut_BCLEAR = mbmc_o.BCLEAR;
  assign regOut_CMODE = mbmc_o.CMODE;

endmodule
