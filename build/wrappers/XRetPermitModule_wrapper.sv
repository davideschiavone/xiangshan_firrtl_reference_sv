// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `XRetPermitModule` of class `XRetPermitModule`.
// Outer port list mirrors reference_sv/XRetPermitModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `XRetPermitModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
module XRetPermitModule_wrapper (
  input  logic [1:0] io_in_privState_PRVM,
  input  logic io_in_privState_V,
  input  logic io_in_debugMode,
  input  logic io_in_xRet_mnret,
  input  logic io_in_xRet_mret,
  input  logic io_in_xRet_sret,
  input  logic io_in_xRet_dret,
  input  logic io_in_status_tsr,
  input  logic io_in_status_vtsr,
  output logic io_out_Xret_EX_II,
  output logic io_out_Xret_EX_VI,
  output logic io_out_hasLegalMNret,
  output logic io_out_hasLegalMret,
  output logic io_out_hasLegalSret,
  output logic io_out_hasLegalDret
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  csr_common_pkg::exception_pair_t Xret_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::xret_io_t xRet_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::status_io_t status_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::priv_state_t privState_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign privState_i.PRVM = io_in_privState_PRVM;
  assign privState_i.V = io_in_privState_V;
  assign xRet_i.mnret = io_in_xRet_mnret;
  assign xRet_i.mret = io_in_xRet_mret;
  assign xRet_i.sret = io_in_xRet_sret;
  assign xRet_i.dret = io_in_xRet_dret;
  assign status_i.tsr = io_in_status_tsr;
  assign status_i.vtsr = io_in_status_vtsr;
  // (FIRRTL DCE'd `status_mstatusFSOff` — readable doesn't consume it; tie to 0)
  assign status_i.mstatusFSOff = '0;
  // (FIRRTL DCE'd `status_mstatusVSOff` — readable doesn't consume it; tie to 0)
  assign status_i.mstatusVSOff = '0;
  // (FIRRTL DCE'd `status_tvm` — readable doesn't consume it; tie to 0)
  assign status_i.tvm = '0;
  // (FIRRTL DCE'd `status_vgein` — readable doesn't consume it; tie to 0)
  assign status_i.vgein = '0;
  // (FIRRTL DCE'd `status_vsstatusFSOff` — readable doesn't consume it; tie to 0)
  assign status_i.vsstatusFSOff = '0;
  // (FIRRTL DCE'd `status_vsstatusVSOff` — readable doesn't consume it; tie to 0)
  assign status_i.vsstatusVSOff = '0;
  // (FIRRTL DCE'd `status_vtvm` — readable doesn't consume it; tie to 0)
  assign status_i.vtvm = '0;

  // ---- Inner design (the LLM-authored readable) ----
  XRetPermitModule u_inner (
    .debugMode_i(io_in_debugMode),
    .hasLegalMNret_o(io_out_hasLegalMNret),
    .hasLegalMret_o(io_out_hasLegalMret),
    .hasLegalSret_o(io_out_hasLegalSret),
    .hasLegalDret_o(io_out_hasLegalDret),
    .xRet_i(xRet_i),
    .status_i(status_i),
    .privState_i(privState_i),
    .Xret_o(Xret_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_out_Xret_EX_II = Xret_o.EX_II;
  assign io_out_Xret_EX_VI = Xret_o.EX_VI;

endmodule
