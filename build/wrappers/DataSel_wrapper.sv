// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `DataSel` of class `DataSel`.
// Outer port list mirrors reference_sv/DataSel.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `DataSel` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================

module DataSel_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic [63:0] io_in_0,
  input  logic [63:0] io_in_1,
  input  logic [1:0] io_sel_0,
  input  logic [1:0] io_sel_1,
  input  logic io_en_0,
  input  logic io_en_1,
  output logic [63:0] io_out_0,
  output logic [63:0] io_out_1
);

  // Unpacked-array wire — Chisel Vec(2) input port.
  logic [64-1:0] in_i [2];
  // Unpacked-array wire — Chisel Vec(2) input port.
  logic [2-1:0] sel_i [2];
  // Unpacked-array wire — Chisel Vec(2) input port.
  logic en_i [2];
  // Unpacked-array wire — Chisel Vec(2) output port.
  logic [64-1:0] out_o [2];

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign in_i[0] = io_in_0;
  assign in_i[1] = io_in_1;
  assign sel_i[0] = io_sel_0;
  assign sel_i[1] = io_sel_1;
  assign en_i[0] = io_en_0;
  assign en_i[1] = io_en_1;

  // ---- Inner design (the LLM-authored readable) ----
  DataSel u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .in_i(in_i),
    .sel_i(sel_i),
    .en_i(en_i),
    .out_o(out_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_out_0 = out_o[0];
  assign io_out_1 = out_o[1];

endmodule
