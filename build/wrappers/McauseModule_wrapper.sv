// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `McauseModule` of class `ScauseModule`.
// Outer port list mirrors reference_sv/McauseModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `ScauseModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import scause_pkg::*;
import csr_common_pkg::*;
import trap_event_pkg::*;
module McauseModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_Interrupt,
  output logic [62:0] regOut_ExceptionCode,
  input  logic trapToM_mcause_valid,
  input  logic trapToM_mcause_bits_Interrupt,
  input  logic [62:0] trapToM_mcause_bits_ExceptionCode
);

  // Output struct wire carrying the readable's CSR state.
  scause_pkg::scause_t scause_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  trap_event_pkg::trap_cause_event_t trap_cause_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign trap_cause_i.valid = trapToM_mcause_valid;
  assign trap_cause_i.bits_Interrupt = trapToM_mcause_bits_Interrupt;
  assign trap_cause_i.bits_ExceptionCode = trapToM_mcause_bits_ExceptionCode;

  // ---- Inner design (the LLM-authored readable) ----
  ScauseModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .trap_cause_i(trap_cause_i),
    .scause_o(scause_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_Interrupt = scause_o.Interrupt;
  assign regOut_ExceptionCode = scause_o.ExceptionCode;

endmodule
