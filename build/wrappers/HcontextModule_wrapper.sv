// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `HcontextModule` of class `HcontextModule`.
// Outer port list mirrors reference_sv/HcontextModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HcontextModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
module HcontextModule_wrapper (
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [13:0] rdata,
  output logic [13:0] regOut_HCONTEXT,
  input  logic [13:0] fromMcontext_HCONTEXT,
  output logic toMcontext_valid,
  output logic [13:0] toMcontext_bits_HCONTEXT
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  csr_common_pkg::to_mcontext_t toMcontext_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  HcontextModule u_inner (
    .rdata_o(rdata),
    .regOut_HCONTEXT_o(regOut_HCONTEXT),
    .fromMcontext_HCONTEXT_i(fromMcontext_HCONTEXT),
    .w_i(w_i),
    .toMcontext_o(toMcontext_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign toMcontext_valid = toMcontext_o.valid;
  assign toMcontext_bits_HCONTEXT = toMcontext_o.bits_HCONTEXT;

endmodule
