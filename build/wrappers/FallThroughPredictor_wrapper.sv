// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `FallThroughPredictor` of class `FallThroughPredictor`.
// Outer port list mirrors reference_sv/FallThroughPredictor.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `FallThroughPredictor` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import prediction_pkg::*;
module FallThroughPredictor_wrapper (
  input  logic clock,
  input  logic io_stageCtrl_s0_fire,
  input  logic [48:0] io_startPc_addr,
  output logic [3:0] io_prediction_cfiPosition,
  output logic [48:0] io_prediction_target_addr
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  prediction_pkg::prediction_t prediction_o;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  // (no input struct)

  // ---- Inner design (the LLM-authored readable) ----
  FallThroughPredictor u_inner (
    .clk_i(clock),
    .stageCtrl_s0_fire_i(io_stageCtrl_s0_fire),
    .startPc_addr_i(io_startPc_addr),
    .prediction_o(prediction_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_prediction_cfiPosition = prediction_o.cfiPosition;
  assign io_prediction_target_addr = prediction_o.target_addr;

endmodule
