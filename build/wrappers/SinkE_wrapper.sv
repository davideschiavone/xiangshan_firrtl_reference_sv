// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `SinkE` of class `SinkE`.
// Outer port list mirrors reference_sv/SinkE.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `SinkE` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import sinke_pkg::*;
module SinkE_wrapper (
  input  logic io_e_valid,
  input  logic [3:0] io_e_bits_sink,
  output logic io_resp_valid,
  output logic [3:0] io_resp_bits_sink
);

  // Output struct wire carrying a write-forward bundle to a peer CSR.
  sinke_pkg::sink_e_bits_t resp_bits_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  sinke_pkg::sink_e_bits_t e_bits_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign e_bits_i.sink = io_e_bits_sink;

  // ---- Inner design (the LLM-authored readable) ----
  SinkE u_inner (
    .e_valid_i(io_e_valid),
    .resp_valid_o(io_resp_valid),
    .e_bits_i(e_bits_i),
    .resp_bits_o(resp_bits_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign io_resp_bits_sink = resp_bits_o.sink;

endmodule
