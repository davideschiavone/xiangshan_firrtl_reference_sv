// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `HvictlModule` of class `HvictlModule`.
// Outer port list mirrors reference_sv/HvictlModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HvictlModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import hvictl_pkg::*;
import csr_common_pkg::*;
module HvictlModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_VTI,
  output logic [11:0] regOut_IID,
  output logic regOut_DPR,
  output logic regOut_IPRIOM,
  output logic [7:0] regOut_IPRIO
);

  // Output struct wire carrying the readable's CSR state.
  hvictl_pkg::hvictl_t hvictl_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  HvictlModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .hvictl_o(hvictl_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_VTI = hvictl_o.VTI;
  assign regOut_IID = hvictl_o.IID;
  assign regOut_DPR = hvictl_o.DPR;
  assign regOut_IPRIOM = hvictl_o.IPRIOM;
  assign regOut_IPRIO = hvictl_o.IPRIO;

endmodule
