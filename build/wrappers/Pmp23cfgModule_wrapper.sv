// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Pmp23cfgModule` of class `Pmp0cfgModule`.
// Outer port list mirrors reference_sv/Pmp23cfgModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `Pmp0cfgModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import pmp0cfg_pkg::*;
import csr_common_pkg::*;
module Pmp23cfgModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [7:0] rdata,
  output logic regOut_R,
  output logic regOut_W,
  output logic regOut_X,
  output logic [1:0] regOut_A,
  output logic regOut_L
);

  // Output struct wire carrying the readable's CSR state.
  pmp0cfg_pkg::pmp0cfg_t pmp0cfg_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  Pmp0cfgModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .pmp0cfg_o(pmp0cfg_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_R = pmp0cfg_o.R;
  assign regOut_W = pmp0cfg_o.W;
  assign regOut_X = pmp0cfg_o.X;
  assign regOut_A = pmp0cfg_o.A;
  assign regOut_L = pmp0cfg_o.L;

endmodule
