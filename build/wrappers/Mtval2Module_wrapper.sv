// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `Mtval2Module` of class `StvalModule`.
// Outer port list mirrors reference_sv/Mtval2Module.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `StvalModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import trap_event_pkg::*;
module Mtval2Module_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic [63:0] regOut_ALL,
  input  logic trapToM_mtval2_valid,
  input  logic [63:0] trapToM_mtval2_bits_ALL
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  trap_event_pkg::trap_tval_event_t trap_tval_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign trap_tval_i.valid = trapToM_mtval2_valid;
  assign trap_tval_i.bits_ALL = trapToM_mtval2_bits_ALL;

  // ---- Inner design (the LLM-authored readable) ----
  StvalModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .regOut_ALL_o(regOut_ALL),
    .w_i(w_i),
    .trap_tval_i(trap_tval_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
