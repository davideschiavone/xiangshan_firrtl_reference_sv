// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `MflushpwrModule` of class `MflushpwrModule`.
// Outer port list mirrors reference_sv/MflushpwrModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `MflushpwrModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import mflushpwr_pkg::*;
import csr_common_pkg::*;
module MflushpwrModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_FLUSH_L2_ENABLE,
  output logic regOut_L2_FLUSH_DONE,
  input  logic l2FlushDone
);

  // Output struct wire carrying the readable's CSR state.
  mflushpwr_pkg::mflushpwr_t mflushpwr_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  MflushpwrModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .l2FlushDone_i(l2FlushDone),
    .w_i(w_i),
    .mflushpwr_o(mflushpwr_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_FLUSH_L2_ENABLE = mflushpwr_o.FLUSH_L2_ENABLE;
  assign regOut_L2_FLUSH_DONE = mflushpwr_o.L2_FLUSH_DONE;

endmodule
