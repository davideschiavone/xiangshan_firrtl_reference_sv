// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `HenvcfgModule` of class `HenvcfgModule`.
// Outer port list mirrors reference_sv/HenvcfgModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `HenvcfgModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import henvcfg_pkg::*;
import csr_common_pkg::*;
import menvcfg_pkg::*;
module HenvcfgModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  output logic regOut_STCE,
  output logic regOut_PBMTE,
  output logic regOut_DTE,
  output logic [1:0] regOut_PMM,
  output logic regOut_CBZE,
  output logic regOut_CBCFE,
  output logic [1:0] regOut_CBIE,
  input  logic menvcfg_STCE,
  input  logic menvcfg_PBMTE,
  input  logic menvcfg_DTE
);

  // Output struct wire carrying the readable's CSR state.
  henvcfg_pkg::henvcfg_t henvcfg_o;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  menvcfg_pkg::menvcfg_t menvcfg_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign menvcfg_i.STCE = menvcfg_STCE;
  assign menvcfg_i.PBMTE = menvcfg_PBMTE;
  assign menvcfg_i.DTE = menvcfg_DTE;
  // (FIRRTL DCE'd `menvcfg_CBCFE` — readable doesn't consume it; tie to 0)
  assign menvcfg_i.CBCFE = '0;
  // (FIRRTL DCE'd `menvcfg_CBIE` — readable doesn't consume it; tie to 0)
  assign menvcfg_i.CBIE = '0;
  // (FIRRTL DCE'd `menvcfg_CBZE` — readable doesn't consume it; tie to 0)
  assign menvcfg_i.CBZE = '0;
  // (FIRRTL DCE'd `menvcfg_CDE` — readable doesn't consume it; tie to 0)
  assign menvcfg_i.CDE = '0;
  // (FIRRTL DCE'd `menvcfg_PMM` — readable doesn't consume it; tie to 0)
  assign menvcfg_i.PMM = '0;

  // ---- Inner design (the LLM-authored readable) ----
  HenvcfgModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .w_i(w_i),
    .menvcfg_i(menvcfg_i),
    .henvcfg_o(henvcfg_o)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  assign regOut_STCE = henvcfg_o.STCE;
  assign regOut_PBMTE = henvcfg_o.PBMTE;
  assign regOut_DTE = henvcfg_o.DTE;
  assign regOut_PMM = henvcfg_o.PMM;
  assign regOut_CBZE = henvcfg_o.CBZE;
  assign regOut_CBCFE = henvcfg_o.CBCFE;
  assign regOut_CBIE = henvcfg_o.CBIE;

endmodule
