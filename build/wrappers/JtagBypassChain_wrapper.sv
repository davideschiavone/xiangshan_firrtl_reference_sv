// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `JtagBypassChain` of class `JtagBypassChain`.
// Outer port list mirrors reference_sv/JtagBypassChain.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `JtagBypassChain` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import jtag_shifter_pkg::*;
module JtagBypassChain_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_chainIn_shift,
  input  logic io_chainIn_data,
  input  logic io_chainIn_capture,
  input  logic io_chainIn_update,
  output logic io_chainOut_data
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  jtag_shifter_pkg::shifter_io_t chainIn_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign chainIn_i.shift = io_chainIn_shift;
  assign chainIn_i.data = io_chainIn_data;
  assign chainIn_i.capture = io_chainIn_capture;
  assign chainIn_i.update = io_chainIn_update;

  // ---- Inner design (the LLM-authored readable) ----
  JtagBypassChain u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .chainOut_data_o(io_chainOut_data),
    .chainIn_i(chainIn_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
