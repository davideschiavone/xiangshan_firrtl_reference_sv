// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `JtagStateMachine` of class `JtagStateMachine`.
// Outer port list mirrors reference_sv/JtagStateMachine.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, it
// instantiates the parameterized readable `JtagStateMachine` with the
// pin values that distinguish this variant from its siblings.
// =====================================================================

module JtagStateMachine_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic io_tms,
  output logic [3:0] io_currState
);

  JtagStateMachine u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .tms_i(io_tms),
    .currState_o(io_currState)
  );

endmodule
