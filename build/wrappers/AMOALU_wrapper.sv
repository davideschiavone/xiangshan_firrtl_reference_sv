// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py — DO NOT EDIT BY HAND
//
// Verification harness for module AMOALU. Outer port list mirrors the
// FIRRTL emission exactly so it can be substituted into the FIRRTL build
// for SEC and full-system co-simulation. Inside, it packs / unpacks the
// typed bundle (defined in readable_sv/packages/amoalu_pkg.sv)
// and instantiates the real hand-written design (AMOALU).
//
// Package imports are at $unit (compilation-unit) scope rather than in
// the module header — yosys 0.64's read_verilog rejects module-header
// imports (SV-2017 syntax), and the $unit form is equivalent for our
// purposes.
// =====================================================================
import amoalu_pkg::*;
import xiangshan_mem_op_pkg::*;
module AMOALU_wrapper (
  input  logic [7:0] io_mask,
  input  logic [4:0] io_cmd,
  input  logic [63:0] io_lhs,
  input  logic [63:0] io_rhs,
  output logic [63:0] io_out
);

  // Packed bundles for the inner readable module.
  amoalu_in_t   amoalu_i;
  amoalu_out_t  amoalu_o;

  // ---- Pack flat inputs into typed bundle ----
  always_comb amoalu_i.mask = io_mask;
  always_comb amoalu_i.cmd = mem_op_e'(io_cmd);
  always_comb amoalu_i.lhs = io_lhs;
  always_comb amoalu_i.rhs = io_rhs;

  // ---- Inner design (the LLM-authored readable implementation) ----
  AMOALU u_inner (
    .amoalu_i (amoalu_i),
    .amoalu_o (amoalu_o)
  );

  // ---- Unpack typed outputs back to flat ----
  always_comb io_out = amoalu_o.out;

endmodule
