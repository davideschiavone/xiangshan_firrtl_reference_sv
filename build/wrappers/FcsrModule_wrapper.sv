// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `FcsrModule` of class `FcsrModule`.
// Outer port list mirrors reference_sv/FcsrModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `FcsrModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
import rob_commit_csr_pkg::*;
module FcsrModule_wrapper (
  input  logic clock,
  input  logic reset,
  input  logic w_wen,
  input  logic [63:0] w_wdata,
  output logic [63:0] rdata,
  input  logic robCommit_fflags_valid,
  input  logic [4:0] robCommit_fflags_bits,
  input  logic wAliasFflags_wen,
  input  logic [63:0] wAliasFflags_wdata,
  input  logic wAliasFfm_wen,
  input  logic [63:0] wAliasFfm_wdata,
  output logic [4:0] fflags,
  output logic [2:0] frm,
  output logic [4:0] fflagsRdata,
  output logic [2:0] frmRdata
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t w_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t wAliasFflags_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::csr_w_t wAliasFfm_i;
  // Input struct wire carrying a peer CSR's flat-packed bundle.
  rob_commit_csr_pkg::robCommit_fcsr_subset_t robCommit_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign w_i.wen = w_wen;
  assign w_i.wdata = w_wdata;
  assign robCommit_i.fflags_valid = robCommit_fflags_valid;
  assign robCommit_i.fflags_bits = robCommit_fflags_bits;
  assign wAliasFflags_i.wen = wAliasFflags_wen;
  assign wAliasFflags_i.wdata = wAliasFflags_wdata;
  assign wAliasFfm_i.wen = wAliasFfm_wen;
  assign wAliasFfm_i.wdata = wAliasFfm_wdata;

  // ---- Inner design (the LLM-authored readable) ----
  FcsrModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .fflags_o(fflags),
    .frm_o(frm),
    .fflagsRdata_o(fflagsRdata),
    .frmRdata_o(frmRdata),
    .w_i(w_i),
    .wAliasFflags_i(wAliasFflags_i),
    .wAliasFfm_i(wAliasFfm_i),
    .robCommit_i(robCommit_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
