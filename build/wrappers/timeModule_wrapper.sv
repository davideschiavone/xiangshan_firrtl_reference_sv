// =====================================================================
// AUTO-GENERATED BY scripts/wrappergen.py --class — DO NOT EDIT BY HAND
//
// Wrapper for variant `timeModule` of class `timeModule`.
// Outer port list mirrors reference_sv/timeModule.sv exactly so
// it can be substituted into the FIRRTL build for SEC. Inside, the
// wrapper instantiates the readable `timeModule` and packs/unpacks
// struct ports per sv_style rule 7.
// =====================================================================
import csr_common_pkg::*;
module timeModule_wrapper (
  input  logic clock,
  input  logic reset,
  output logic [63:0] rdata,
  input  logic mHPM_time_valid,
  input  logic [63:0] mHPM_time_bits,
  input  logic v,
  input  logic nextV,
  input  logic [63:0] htimedelta,
  input  logic debugModeStopTime,
  output logic updated,
  output logic [63:0] stime,
  output logic [63:0] vstime
);

  // Input struct wire carrying a peer CSR's flat-packed bundle.
  csr_common_pkg::valid_uint64_t mHPM_time_i;

  // ---- Pack peer-CSR flat FIRRTL inputs into the input struct ----
  assign mHPM_time_i.valid = mHPM_time_valid;
  assign mHPM_time_i.bits = mHPM_time_bits;

  // ---- Inner design (the LLM-authored readable) ----
  timeModule u_inner (
    .clk_i(clock),
    .rst_i(reset),
    .rdata_o(rdata),
    .v_i(v),
    .nextV_i(nextV),
    .htimedelta_i(htimedelta),
    .debugModeStopTime_i(debugModeStopTime),
    .updated_o(updated),
    .stime_o(stime),
    .vstime_o(vstime),
    .mHPM_time_i(mHPM_time_i)
  );

  // ---- Unpack the CSR struct into the flat FIRRTL `regOut_<F>` ----
  // (no regOut_<F> ports on this variant)

endmodule
